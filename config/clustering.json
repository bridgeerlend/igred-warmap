{
  "$comment": "Clustering collapses many articles about one incident into one map point. Deterministic code only — no model is involved in deciding what is the same event.",
  "schemaVersion": 1,
  "gridDegrees": 0.25,
  "sameDayOnly": true,
  "mergeAcrossCategories": false,
  "maxEventsPerCluster": 500
}
